# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: deepfake-detect
metadata:
  template: deepfake-detect@0.0.1-beta

services:
  backend:
    project: ./backend
    language: python
    host: containerapp
    docker:
      path: ./backend/Dockerfile
      context: ./backend

  frontend:
    project: ./frontend
    language: js
    host: containerapp
    docker:
      path: ./frontend/Dockerfile
      context: ./frontend
